browser: capture MyMiniFactory links/artwork for 93 models; add the agent message channel

reference/incoming.tsv is ready for merge-reference: the 83 numbered models
in models.tsv plus the 10 August extras directories, each with its object
page (mmf) and a 1000x1000 webp URL (artwork); all 93 image URLs were
load-tested in the browser. reference/mmf-links.tsv is the full 113-object
dump of the Paizo Printables library.

tools/messages.py and reference/messages-from-{browser,ledger}.jsonl are the
new append-only channel between the two agents (one writer per file, no
locks, no unlinks — the browser side of the folder cannot delete files, which
is why git and SQLite fail there). CLAUDE.md gains "Talking to the other
agent" covering the channel, commit practice and per-file ownership, and
loses "commit only if asked"; the handoff section now points at the channel.
reference/HANDOFF-NOTES.md is marked superseded.

Open items are in the channel: browser-20260911-2 (cmd_artwork keying),
-3 (this commit), -4 (extras base sizes, for the user).
